# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /new_event.html                           controllers.CallOperatorController.index()

POST    /event                     controllers.CallOperatorController.addEvent()
POST    /subscribe				   controllers.SubscribeController.addEvent()

GET     /events                    controllers.CallOperatorController.getEvents()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
